# LLM
openai>=1.40.0
numpy>=1.26.0

# Telegram
python-telegram-bot>=21.0.0

# Voice — STT (CPU mode works fine, just slower)
faster-whisper>=1.0.0
pyaudio>=0.2.14

# Voice — Wake word (CPU via ONNX)
openwakeword>=0.6.0
onnxruntime>=1.18.0

# Voice — TTS
piper-tts>=1.2.0

# GitHub PR creation + webhook server
aiohttp>=3.9.0

# PC control
pyautogui>=0.9.54

# Env
python-dotenv>=1.0.0
